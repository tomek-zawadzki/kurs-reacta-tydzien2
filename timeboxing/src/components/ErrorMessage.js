import React from "react";

class ErrorMessage extends React.Component {
  render() {
    if (hasError === true) {
      message;
    } else {
      children;
    }
  }
}

export default ErrorMessage;
